!!!
%html
  %head
    %meta(charset="utf-8")
    %title= controller.controller_name + ': ' + controller.action_name
    %script(src="http://www.google.com/jsapi")
    :javascript
      google.load("jquery", "1.3.2");
      google.load("jqueryui", "1.7.2");
    = sprockets_include_tag
    = stylesheet_link_tag 'screen'
  %body
    .container
      .span-24.last
        - if logged_in?
          %ul.navigation{ :class => controller.controller_name }
            %li.books= link_to "Books", books_path
            %li.users= link_to "Users", users_path
      .span-24.last
        = render_flash
        = yield
      %footer.span-24.last
        %p
          -if logged_in?
            Signed in as
            = link_to h(current_user.name), account_path
            = '(' + link_to("Log out", user_session_path, :method => :delete) + ')'
        

