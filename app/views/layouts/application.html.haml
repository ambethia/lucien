!!!
%html
  %head
    %meta(charset="utf-8")
    %title= controller.controller_name + ': ' + controller.action_name
    %script(src="http://www.google.com/jsapi")
    :javascript
      google.load("jquery", "1.3.2");
      google.load("jqueryui", "1.7.2");
    = javascript_include_tag %w(jquery.humanmsg.easing.min application)
    = stylesheet_link_tag :all, :recursive => true
  %body
    .container
      .span-24.last
        - if logged_in?
          %ul.navigation{ :class => controller.controller_name }
            %li.books= link_to "Books", books_path
      .span-24.last
        = render_flash
        = yield
      %footer.span-24.last
        = render :partial => 'shared/user_status'
