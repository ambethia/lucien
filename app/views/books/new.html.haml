%h2 New Book

- form_for(@book, :html => { :multipart => true }) do |form|
  = form.error_messages
  %p
    = form.label :document
    %br
    = form.file_field :document
  %p
    %button(type="Submit" class="add") Create
    = link_to 'Cancel', books_path, :class => 'button'